function initDynamicPlanner(){const app=document.querySelector('#app');if(!app||!window.cityDatabase)return;const saved=JSON.parse(localStorage.getItem('tripease_state_v1')||'{}');const state=Object.assign({source:'',destination:'',departure:'',returnDate:'',adults:2,children:0,budget:15000,currency:'INR'},saved);const picker=(id,label,value,placeholder)=>`<div class="field city-field"><label>${label} *</label><div class="city-input-wrap"><input id="${id}" class="city-picker" value="${value||''}" placeholder="${placeholder}" autocomplete="off" required><button type="button" class="city-arrow" aria-label="Show cities">⌄</button><div class="city-suggestions" id="${id}Suggestions"></div></div></div>`;app.innerHTML=`<header class="navbar"><a class="brand" href="index.html"><span class="brand-mark">✈</span><span>Trip<span class="brand-accent">Ease</span></span></a><nav class="nav-links"><a href="index.html">Home</a><a class="active" href="planner.html">Plan Trip</a><a href="explore.html">Explore</a><a href="my-trips.html">My Trips</a><a href="budget.html">Budget</a></nav></header><main class="app-shell"><div class="page-head"><div><div class="eyebrow">TRIPEASE · SMART TRAVEL</div><h1>Create your trip</h1><p>Select your starting city and destination from the TripEase city database.</p></div></div><div class="progress"><span class="active">01 Details</span><span>02 Preferences</span><span>03 Transport</span><span>04 Stay</span><span>05 Places</span><span>06 Itinerary</span><span>07 Budget</span><span>08 Summary</span></div><form id="dynamicTripForm" class="panel"><h2>Trip Details</h2><div class="form-grid">${picker('source','Starting city',state.source,'Click arrow or type first letters...')}${picker('destination','Destination city',state.destination,'Click arrow or type first letters...')}<div class="field"><label>Departure date *</label><input id="departure" type="date" value="${state.departure||''}" required></div><div class="field"><label>Return date *</label><input id="returnDate" type="date" value="${state.returnDate||''}" required></div><div class="field"><label>Adults</label><input id="adults" type="number" min="1" value="${state.adults||2}"></div><div class="field"><label>Children</label><input id="children" type="number" min="0" value="${state.children||0}"></div><div class="field"><label>Total budget (₹)</label><input id="budget" type="number" min="1" value="${state.budget||15000}"></div><div class="field"><label>Currency</label><select id="currency"><option>INR</option><option>USD</option><option>EUR</option></select></div></div><div id="cityError"></div><div class="action-row"><span></span><button class="btn primary">Continue →</button></div></form></main><footer><div class="footer-bottom">© 2026 TripEase · Demo city database · Real-time APIs can be integrated later.</div></footer>`;const setup=(id)=>{const input=document.getElementById(id),box=document.getElementById(id+'Suggestions'),arrow=input.parentElement.querySelector('.city-arrow');const render=()=>{const q=input.value.trim().toLowerCase();const matches=(q?cityDatabase.filter(c=>(c.city+' '+c.state+' '+c.categories.join(' ')).toLowerCase().includes(q)):cityDatabase).slice(0,10);box.innerHTML=matches.map(c=>`<button type="button" data-city="${c.city}"><span>${c.city}</span><small>${c.state} · ${c.categories.join(' · ')}</small></button>`).join('');box.classList.add('open')};input.addEventListener('input',render);input.addEventListener('focus',()=>{if(!input.value)render()});arrow.addEventListener('click',()=>{input.focus();render()});box.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;input.value=b.dataset.city;box.classList.remove('open')});document.addEventListener('click',e=>{if(!input.parentElement.contains(e.target))box.classList.remove('open')})};setup('source');setup('destination');document.getElementById('dynamicTripForm').onsubmit=e=>{e.preventDefault();const source=cityDatabase.find(c=>c.city.toLowerCase()===document.getElementById('source').value.trim().toLowerCase());const destination=cityDatabase.find(c=>c.city.toLowerCase()===document.getElementById('destination').value.trim().toLowerCase());const d=new Date(document.getElementById('departure').value),r=new Date(document.getElementById('returnDate').value);if(!source||!destination){document.getElementById('cityError').innerHTML='<div class="danger">Please select both cities from the list.</div>';return}if(source.city===destination.city){document.getElementById('cityError').innerHTML='<div class="danger">Starting city and destination must be different.</div>';return}if(r<d){document.getElementById('cityError').innerHTML='<div class="danger">Return date cannot be before departure.</div>';return}Object.assign(state,{source:source.city,destination:destination.city,departure:document.getElementById('departure').value,returnDate:document.getElementById('returnDate').value,adults:+document.getElementById('adults').value,children:+document.getElementById('children').value,budget:+document.getElementById('budget').value,currency:document.getElementById('currency').value,places:[],transport:null,hotel:null});localStorage.setItem('tripease_state_v1',JSON.stringify(state));location.href='preferences.html'}};initDynamicPlanner();